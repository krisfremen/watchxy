name: CD # Continuous Deployment

on:
  push:
    tags:
      - '[v]?[0-9]+.[0-9]+.[0-9]+'
      - '[v]?[0-9]+.[0-9]+.[0-9]+-[a-z]+.[0-9]+'

jobs:
  publish-binaries:
    uses: ./.github/workflows/publish-binaries.yml
    with:
      ref: ${{ github.ref_name }}
    secrets: inherit
